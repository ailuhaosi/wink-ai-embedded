/** UI push interval for the wasm sim loop (~60Hz). */
export const SIM_UI_TICK_MS = 16;

/** Ring-buffer cap for worker console logs mirrored into the UI. */
export const MAX_SIM_LOG_ENTRIES = 1000;
